<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiendadecomida">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiendaDeComida">
        <activity
            android:name=".activities.PedidoHechoActivity"
            android:exported="false" />
        <activity
            android:name=".activities.perfilActivity"
            android:exported="false" />
        <activity
            android:name=".activities.NavCategoriaActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DetalleProductoActivity"
            android:exported="false" />
        <activity
            android:name=".activities.VerTodoActivity"
            android:exported="false" />
        <activity
            android:name=".activities.RegistrarActivity"
            android:exported="true">

            <!--
            intent-filter>
                <action android:name="android.intent.action.MAIN" />
android:hardwareAccelerated="false"
android:largeHeap="true"
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter
            -->
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TiendaDeComida.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>